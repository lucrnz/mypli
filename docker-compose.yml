version: '3.1'

services:
  api:
    build:
      context: .
    restart: always
    environment:
      - PORT=${PORT}
      - SECRET_KEY=${SECRET_KEY}
    ports:
      - ${HOST}:${PORT}:${PORT}
    volumes:
      - ./cfg:/app/cfg
